//Criando interface para calcular bônus de funcionarios utilizando o método 'calcularBonus' para quando chamadas por cada classe, ativar o método.
public interface Funcionario {
	 double calcularBonus();
	
//Método interface para exibição formatada quando puxadas por cada classe. 
	 String exibicao();
}
